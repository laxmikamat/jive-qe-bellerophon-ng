jms.broker.url=vm://embedded-broker?create=false
git.clone.root.dir=${GIT_CLONE_ROOT_DIR:/tmp}
understand.db.root.dir=${UNDERSTAND_DB_ROOT_DIR:/tmp}
understand.exec=${UNDERSTAND_EXEC:und}

#spring.datasource.url=jdbc:hsqldb:mem:repos
spring.datasource.url=jdbc:h2:/tmp/repos-test
spring.datasource.username=SA
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=create
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
